# This file makes the `backend` directory a Python package.
# It is intentionally left empty; the presence of the file allows
# imports such as `import backend.main` which uvicorn requires to
# locate the FastAPI application instance.